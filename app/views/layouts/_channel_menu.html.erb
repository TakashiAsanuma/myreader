<div class="l_channel_menu">
  <nav>
    <ul>
      <% @myreads.each do |myread| %>
        <li><%= link_to "#{myread.channel_name}", "/feed?channel_id=#{myread.channel_id}" %>&nbsp;</li>
      <% end %>
    </ul>
  </nav>
</div>
